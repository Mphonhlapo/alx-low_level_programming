#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Printing lowcase characters'
 * Return: always 0
 */
int main(void)
{
	int n = 97

	while (n < 123)
	{
		putchar(n);
		n++;
	}
	putchar('\n')
	return (0);
}
